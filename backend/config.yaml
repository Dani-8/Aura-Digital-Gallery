server:
  port: 5000
  env: "development"
  apiBasePath: "/api/v1"

database:
  url: ${MONGODB_CONNECT}
  options: 
    useNewUrlParser: true
    useUnifiedTopology: true
    maxPoolSize: 10


security:
  jwt_secret: "aura_digital_secret_2024"
  token_expiry: "24h"
  cors_origin: "http://localhost:3000"

deployment:
  platform: "vercel"
  auto_deploy: true