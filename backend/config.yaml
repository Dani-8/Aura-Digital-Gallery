server:
  port: 5000
  env: "development"
  apiBasePath: "/api/v1"


mongodb:
  url: ${MONGODB_CONNECT}


security:
  jwt_secret: "aura_digital_secret_2024"
  token_expiry: "24h"
  cors_origin: "http://localhost:3000"


deployment:
  platform: "vercel"
  auto_deploy: true
  